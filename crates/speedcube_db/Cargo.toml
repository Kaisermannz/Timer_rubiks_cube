[package]
name = "speedcube_db"
version = "0.1.0"
edition = "2024"

[dependencies]
sqlx = { version = "0.7", features = ["sqlite", "runtime-tokio-rustls"] }
tokio = { version = "1", features = ["full"] }
speedcube_domain = { path = "../speedcube_domain" }
